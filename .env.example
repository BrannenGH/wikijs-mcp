# Optional. Only needed to override where the container's port is published;
# the container itself has no other configuration besides wikijs.env.
# WIKIJS_MCP_BIND_ADDR=0.0.0.0
# WIKIJS_MCP_PORT=5094
